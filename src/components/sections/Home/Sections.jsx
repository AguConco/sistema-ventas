import { Link } from 'react-router-dom'

const Sections = ({sections}) => {

    return (
        <div>
            {sections.map(s => 
            // console.log(s)
            {}
                
            )}
        </div>
    )
}

export default Sections